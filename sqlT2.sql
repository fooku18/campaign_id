SELECT DISTINCT(CATEGORY) FROM ks_eingang WHERE CATEGORY IS NOT NULL AND CATEGORY NOT IN (SELECT * FROM kosten_be)
UNION ALL 
SELECT DISTINCT(CATEGORY) FROM hs_reporting WHERE CATEGORY IS NOT NULL AND CATEGORY NOT IN (SELECT * FROM kosten_be);